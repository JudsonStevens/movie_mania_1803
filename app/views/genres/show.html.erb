<%= @genre.name %>

<% @movies.each do |movie| %>
  <%= movie.title %>
<% end %>

<h1>Average Rating for All Movies in this Genre:</h1>
<%= @genre.average_rating %>

<h1>Title and Rating of Highest Rated Movie:</h1>
<%= @genre.highest_rated_movie.title %>
<%= @genre.highest_rated_movie.rating %>